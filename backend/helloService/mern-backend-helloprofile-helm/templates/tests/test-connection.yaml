apiVersion: v1
kind: Pod
metadata:
  namespace: mern-app
  name: "{{ include "mern-backend-helloprofile-helm.fullname" . }}-test-connection"
  labels:
    {{- include "mern-backend-helloprofile-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mern-backend-helloprofile-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
