apiVersion: v1
kind: Service
metadata:
  namespace: mern-app
  name: {{ include "mern_frontend_service.fullname" . }}
  labels:
    {{- include "mern_frontend_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if eq .Values.service.type "NodePort" }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "mern_frontend_service.selectorLabels" . | nindent 4 }}
