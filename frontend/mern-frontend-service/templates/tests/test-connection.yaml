apiVersion: v1
kind: Pod
metadata:
  namespace: mern-app
  name: "{{ include "mern_frontend_service.fullname" . }}-test-connection"
  labels:
    {{- include "mern_frontend_service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mern_frontend_service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
